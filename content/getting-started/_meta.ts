import type { MetaRecord } from 'nextra'

const meta: MetaRecord = {
  installation: 'Installation',
  configuration: 'Configuration',
  'first-project': 'Your First Project',
  'migrate-from-legacy': 'Ycode Legacy Shutdown'
}

export default meta
